import { ImpersonateKind, SDKStoreState, UserKind } from '../../redux-types';
declare type GetImpersonate = (state: SDKStoreState) => ImpersonateKind;
declare type GetUser = (state: SDKStoreState) => UserKind;
declare type GetCluster = (state: SDKStoreState) => string;
/**
 * It provides impersonation details from the redux store.
 * @param state the root state
 * @returns The the impersonate state.
 */
export declare const getImpersonate: GetImpersonate;
/**
 * It provides impersonation details from the redux store as a props object.
 * @param state the root state
 * @returns The the impersonation details props object.
 */
export declare const impersonateStateToProps: (state: SDKStoreState) => {
    impersonate: ImpersonateKind;
};
/**
 * It provides user details from the redux store.
 * @param state the root state
 * @returns The the user state.
 */
export declare const getUser: GetUser;
/**
 * It provides current active cluster.
 * @param state the root state
 * @returns The the current active cluster.
 */
export declare const getActiveCluster: GetCluster;
export {};
